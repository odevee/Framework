class Entity:


    def __init__(self, x, y, walkable):
            self.x = x
            self.y = y
            self.walkable = walkable
    # Attribute
    x = 0
    y = 0
    walkable = 0
    def update(self):
        self.x = self.x

    