class world:
    def __init__(self, w, h):
        self.width = w
        self.height = h

    # Array mit Entities
        entities = [[0 for x in range(w)] for y in range(h)]
    entities = [[0 for x in range(w)] for y in range(h)]


